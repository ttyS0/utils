# utils > select-courses

Select courses in HUST. :D

Dependencies: `deps.sh`

---

Tips:

* Delete `cache.json` to regenerate all courses information.

* Delete `subscribed.json` to forget those courses which you have subscribed.